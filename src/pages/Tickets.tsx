import { RoutePlaceholder } from '../components/RoutePlaceholder'

export function Tickets() {
  return (
    <RoutePlaceholder
      title="Mis tickets"
      description="Vista base del listado de boletas. La consulta, filtros y paginacion se implementaran en el Tramo 7."
    />
  )
}
